# Shared runtime directory for the chrony SOCK refclock socket.
# chronyd creates /run/chronos/chrony.sock inside this directory; chronos-gateway
# writes samples to it. If chronyd runs as a different user, adjust ownership so
# both processes can access the socket.
d /run/chronos 0755 chronos chronos -
